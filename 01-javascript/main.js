import "./js/fetch-data.js"
import "./js/apply-button.js"
import "./js/filters.js"
import "./js/devjobs-avatar-element.js"
import "./js/search-bar.js"

// -- Funciones creadas durante el bootcamp, aprendizaje.

/* const botones = document.querySelectorAll(".button-job-apply")
botones.forEach(boton=>{
    boton.addEventListener("click", () => {
    boton.textContenst="¡Aplicado!"
    boton.classList.add("is-applied")
    boton.disabled=true
})
})
 */

/* const searchInput= document.querySelector("#empleos-search-input")

searchInput.addEventListener("input", ()=>{
    console.log(searchInput.value)
})

searchInput.addEventListener("blur", ()=>{
    console.log("se dispara cuando el campo pierde el foco")
})

const searchForm=document.querySelector("#empleos-search-form")

searchForm.addEventListener("submit", ()=>{
    event.preventDefault()
    console.log("submit")
})

document.addEventListener("keydown",()=>{
    console.log(`tecla presionada: ${event.key}`)
}) */